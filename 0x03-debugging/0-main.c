#include "main.h"

/**
 * main - checks function that prints out if an integer is positive or negative
 *
 * Return: returns 0 (success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
