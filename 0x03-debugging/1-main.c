#include <stdio.h>

/**
 * main - creates an infinite loop
 * Return: returns 0 (success)
 */

int main(void)
{
	int i;

	printf("Infinite loop invoming :(\n");

	i = 0;

	/* while (i < 10) */
	/* { */
	/*        putchar(i); */
	/* } */
	
	printf("Infinite loop avoided! \\o/\n");
	
	return (0);
}
